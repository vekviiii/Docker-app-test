pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                dir('client') {
                    sh 'docker build -t my-react:dev .'
                }
            }
        }
        stage('Test') {
            steps {
                sh 'docker run my-react:dev npm run test'
            }
        }
        stage('Deploy') {
            steps {
                sh 'docker run -d -p 5173:5173 my-react:dev'
            }
        }
    }
}